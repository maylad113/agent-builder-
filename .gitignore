# Dependencies are installed to /opt/agentforge via --prefix (see /home/team/shared/skills
# if you need to reinstall). Never commit node_modules or the symlink.
node_modules

# Build output
dist/

# Local SQLite database (lives at ./data/agentforge.db; small, persists on disk)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Logs & misc
*.log
.DS_Store

# Build artifacts / archives
*.zip
ai-agent-builder.zip

# Temporary conversion helper scripts (one-off, not source)
_convert_routes.py
_convert_*.py
